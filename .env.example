# Taxdown Database Configuration
# Copy this file to .env and fill in your actual values
# WARNING: Never commit .env to version control!

# Railway PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# Get this from MJ - it contains sensitive credentials
DATABASE_URL=postgresql://user:password@host:port/database

# Optional: Local development database (comment out for Railway)
# DATABASE_URL_LOCAL=postgresql://postgres:postgres@localhost:5432/taxdown
